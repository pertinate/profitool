import React from 'react';

interface Props { }

function View(props: Props) {
    const { } = props;

    return (
        <div>
            view my team entries
        </div>
    );
}

export default View;
